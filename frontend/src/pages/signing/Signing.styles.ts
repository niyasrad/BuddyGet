import styled from "@emotion/styled";

export const SignNavigate = styled.p`
    font-size: 0.9em;
    font-weight: 300;
    max-width: 100%;
    color: gray;
    margin: 1rem 0;
    user-select: none;

    span {
        cursor: pointer;
        color: var(--colors-primary);
    }
`
